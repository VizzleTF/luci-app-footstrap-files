# LUCI-APP-FOOTSTRAP-FILES

[English](README.md) · **Русский**

[![owfeed](https://img.shields.io/endpoint?url=https://repo.owfeed.org/badge/luci-app-footstrap-files.json)](https://owfeed.org/install/ru/)
[![owfeed](https://img.shields.io/endpoint?url=https://repo.owfeed.org/badge/luci-app-footstrap-files-releases.json)](https://owfeed.org/install/ru/)

Файловый менеджер для LuCI на OpenWrt 24.10 и новее: просмотр файловой системы роутера, загрузка и
скачивание, переименование, копирование, перенос, права и редактор текстовых файлов с подсветкой.
Зависимости — только `luci-base` и `cgi-io`; своего бэкенда у пакета нет.

Написан для темы [Footstrap](https://github.com/VizzleTF/luci-theme-footstrap) и работает под любой
другой: список — стоковая таблица LuCI `<table class="table">`, а стили ограничены собственными
классами `.fsf-*`, без слоёв и без единого `!important`.

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="assets/readme/tiles-dark.png">
  <img src="assets/readme/tiles-light.png" width="100%" alt="Каталог плиткой: сверху панель с полем пути и кнопками «Перейти», «Вверх», «Обновить», «Новая папка», «Новый файл», «Копировать», «Перенести», «Удалить», «Загрузить» и переключателем список/плитка; под ней хлебные крошки; дальше плитки — папки, симлинк в виде папки со стрелкой и по листу на каждое семейство файлов, с расширением на листе и размером под именем. Правым кликом открыто контекстное меню: «Изменить», «Скачать», «Переименовать», «Свойства», «Выбрать», «Выбрать всё», «Копировать», «Перенести», «Удалить».">
</picture>

<details>
<summary>Редактор с открытым поиском и заменой</summary>

<img src="assets/readme/editor-dark.png" width="100%" alt="/etc/config/network в редакторе: номера строк, uci подсвечен собственной грамматикой — config, option и list акцентом темы, тип секции и имя опции обычным текстом, значения в кавычках зелёным, — оба совпадения со словом «interface» выделены, в углу виджет поиска со счётчиком 1 of 2. Лупа, «Сохранить» и «Закрыть» стоят в ряд под редактором.">

</details>

## Установка

Пакет публикуется в фид `owfeed-packages`. Фид добавляется один раз — скрипт ставит ключ,
репозиторий и переживание sysupgrade для обоих:

```sh
wget -qO- https://repo.owfeed.org/subscribe.sh | sh
```

Дальше — по имени:

```sh
apk add luci-app-footstrap-files        # 25.12 и новее
opkg install luci-app-footstrap-files   # 24.10
```

После этого — **Система → Файлы**.

Установка ключа фида означает доверие ко всем именам пакетов в нём, а не только к этому.
На [owfeed.org/install](https://owfeed.org/install/ru/) написано, чем это грозит, и показаны те же
шаги вручную.

Русский язык: apk сам поставит `luci-i18n-footstrap-files-ru` на роутер, где уже есть
`luci-i18n-base-ru`. У opkg условной формы нет, поэтому там его просят по имени.

Если фид роутеру недоступен, те же `.apk` и `.ipk` приложены к каждому
[релизу](../../releases/latest), рядом с каждым файлом — подпись usign.

## Что умеет

- **Смотреть** — хлебные крошки, сортировка по колонкам, список и плитка, путь в адресной строке,
  так что каталог можно положить в закладки
- **Менять** — загрузка (кнопкой или перетаскиванием файла прямо на нужную папку), скачивание,
  переименование, копирование, перенос, удаление, новый файл, новый каталог
- **Права** — режим и владелец: показываются как `drwxr-xr-x root:root`, правятся как `644` и
  `user:group`, рекурсивно — только если поставить галочку
- **Редактировать** — подсветка, номера строк, парные скобки и поиск с заменой под кнопкой-лупой
  рядом с «Сохранить» (и по Ctrl/Cmd+F, где есть клавиатура); файлы до 1 МБ
- **Hex** — любой файл можно открыть побайтно: смещения, шестнадцать колонок, ASCII рядом; набрал
  поверх байта и сохранил. Режим и владелец файла при сохранении не меняются
- **Мышью** — рамка по пустому месту выделяет всё, что задела; файл (или всё выделение) можно
  перетащить на папку, чтобы перенести
- **На телефоне** — список превращается в карточки, а под редактором лежит обычная `<textarea>`, так
  что клавиатура, IME и автозамена — родные

Нет hex-редактора, предпросмотра markdown и растягивания колонок — это те части стокового
`luci-app-filemanager`, которые пакет намеренно не повторяет.

## Выделение

Один клик открывает; выделение нескольких — это режим, потому что именно так это делается пальцем.
Модель взята у iOS Files: касание открывает, долгое нажатие открывает меню, а его «Выбрать»
включает режим, где касание ставит галочку, а тулбар превращается в счётчик с «Копировать»,
«Перенести», «Удалить» и «Готово».

Мыши режим не нужен: Ctrl/Cmd+клик переключает, Shift+клик берёт диапазон, и любое из двух включает
режим само. С клавиатуры: стрелки двигают, Shift+стрелки расширяют, пробел отмечает, Enter
открывает или редактирует, Ctrl/Cmd+A берёт всё, Escape выходит.

## Права доступа

Пакет запрашивает **полный доступ к файловой системе на чтение и запись от root** — ровно то же,
что берёт стоковый файловый менеджер:

```json
"file": { "/*": [ "list", "read", "write", "exec" ] }
```

Это и есть файловый менеджер, и об этом стоит сказать прямо: любой, кто зашёл в LuCI под группой
ACL этого пакета, может прочитать и изменить любой файл на роутере. Настройки, которая бы это
сузила, нет.

Операции, для которых у ubus нет метода — `mkdir`, `mv`, `cp`, `chmod`, `chown` — идут через
`fs.exec` **массивом аргументов** и с `--` перед операндами, никогда не строкой для шелла. Файл с
именем `; reboot;` или `-rf` остаётся аргументом, а не командой и не флагом. Имя из файловой системы
попадает на страницу только как текст: `innerHTML` здесь нет, и CI роняет сборку, если он появится.

## Редактор

[prism-code-editor](https://github.com/jonpyt/prism-code-editor) (MIT) даёт сам редактор и виджет
поиска с заменой — и больше ничего: **17 файлов, 28 КБ на флеше**. Ни цветов, ни грамматик от него
здесь уже нет.

**У uci своя грамматика** (`grammars.js`, 1 КБ). Раньше конфиги подсвечивались как shell — ближайшее
из того, что везла библиотека, и всё равно неправильно: у `config interface 'lan'` в bash нет
ключевых слов, поэтому файл выглядел набором голых слов со строками в кавычках. INI-грамматика
Prism была ещё хуже: на 120 строках `/etc/config/network` она разметила девять штук. Рядом лежит
своя shell-грамматика вместо библиотечной `bash` на 5,7 КБ, описывавшей here-документы и подстановку
процессов для файлов, где нет ни того, ни другого.

**Цвета берутся у темы, а не у GitHub** (`editor.css`). Библиотека везёт светлую и тёмную таблицы
литералов и ждёт, что страница будет их подменять; здесь один файл на 26 экспортных именах, которые
публикует любая тема LuCI, — редактор уходит в тёмный режим вместе со страницей, без выбора и без
второй таблицы стилей. Это ровно то, чего требует
[руководство темы по стилизации приложений](https://github.com/VizzleTF/luci-theme-footstrap/blob/main/docs/luci-app-styling-guide.md).

Стили редактора живут внутри дерева самой вьюхи, поэтому ничто загруженное этой страницей не
дотягивается до остального документа — свойство, из-за которого альтернативы и отпали.

## Сборка

```sh
npm ci                                     # terser и acorn — ими минифицируется собранная нагрузка
tools/t0.sh                                # каждый модуль парсится — до и после jsmin с бильдбота
FOOTSTRAP_VERSION=0.1.0 ./tools/stage.sh   # dist/root, dist/VERSION, dist/scripts, dist/i18n-*
owfeed build                               # dist/noarch/*.apk (25.12) + dist/all/*.ipk (24.10)
./luci-app-footstrap-files/update-po.sh    # пересканировать строки; --check — гейт в CI
```

Стадия сборки минифицирует то, что едет на роутер: terser по модулям страницы и
`tools/minify-css.sh` по таблицам стилей и снова terser — по встроенному редактору как по
ES-модулям, со сверкой имён импортов и экспортов у каждого модуля: их 17, и они импортируют друг
друга по именам. Вся нагрузка ужимается со 110 255 байт до 68 534. Рабочее дерево при этом не
переписывается.

Сборка через OpenWrt SDK тоже работает — `Makefile` здесь обычный пакет `luci.mk`, и минифицирует
там jsmin, потому что на бильдботе нет node — но из неё ничего не выпускается: пакет noarch, а SDK
тратит по пять минут на формат, выкачивая кросс-тулчейн ради `cp`.

## Лицензия

Apache-2.0. Встроенный редактор под MIT, его лицензия лежит рядом с ним:
`htdocs/luci-static/resources/view/footstrap-files/vendor/pce/LICENSE`.
